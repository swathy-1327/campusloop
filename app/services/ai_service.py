def generate_product_description(title, category, item_condition, mode):
    return (
        f"{title} is listed in the {category} category for {mode}. "
        f"It is in {item_condition} condition and suitable for campus use."
    )
